import { jsx, jsxs } from 'react/jsx-runtime';
import { Text } from '../../Text/Text.esm.js';
import { Link } from '../../Link/Link.esm.js';
import { Avatar } from '../../Avatar/Avatar.esm.js';
import { useDefinition } from '../../../hooks/useDefinition/useDefinition.esm.js';
import '../../Avatar/Avatar.module.css.esm.js';
import { CellProfileDefinition } from '../definition.esm.js';
import { Cell } from 'react-aria-components';

const CellProfile = (props) => {
  const { ownProps, restProps } = useDefinition(CellProfileDefinition, props);
  const { classes, src, name, href, description, color } = ownProps;
  return /* @__PURE__ */ jsx(Cell, { className: classes.root, ...restProps, children: /* @__PURE__ */ jsxs("div", { className: classes.cellContentWrapper, children: [
    src && name && /* @__PURE__ */ jsx(Avatar, { src, name, size: "x-small", purpose: "decoration" }),
    /* @__PURE__ */ jsxs("div", { className: classes.cellContent, children: [
      name && href ? /* @__PURE__ */ jsx(Link, { href, children: name }) : /* @__PURE__ */ jsx(Text, { as: "p", variant: "body-medium", color, children: name }),
      description && /* @__PURE__ */ jsx(Text, { variant: "body-medium", color: "secondary", children: description })
    ] })
  ] }) });
};

export { CellProfile };
//# sourceMappingURL=CellProfile.esm.js.map
