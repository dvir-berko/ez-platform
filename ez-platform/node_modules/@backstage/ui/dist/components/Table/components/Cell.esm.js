import { jsx } from 'react/jsx-runtime';
import { Cell as Cell$1 } from 'react-aria-components';
import { useDefinition } from '../../../hooks/useDefinition/useDefinition.esm.js';
import { CellDefinition } from '../definition.esm.js';

const Cell = (props) => {
  const { ownProps, restProps } = useDefinition(CellDefinition, props);
  return /* @__PURE__ */ jsx(Cell$1, { className: ownProps.classes.root, ...restProps });
};
Cell.displayName = "Cell";

export { Cell };
//# sourceMappingURL=Cell.esm.js.map
