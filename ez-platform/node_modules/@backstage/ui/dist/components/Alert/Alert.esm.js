import { jsxs, jsx } from 'react/jsx-runtime';
import { forwardRef, isValidElement } from 'react';
import { ProgressBar } from 'react-aria-components';
import { RiLoader4Line, RiInformationLine, RiAlertLine, RiErrorWarningLine, RiCheckLine } from '@remixicon/react';
import { useDefinition } from '../../hooks/useDefinition/useDefinition.esm.js';
import { AlertDefinition } from './definition.esm.js';

const Alert = forwardRef(
  (props, ref) => {
    const { ownProps, restProps, dataAttributes, utilityStyle } = useDefinition(
      AlertDefinition,
      props
    );
    const {
      classes,
      status,
      icon,
      loading,
      customActions,
      title,
      description,
      style
    } = ownProps;
    const getStatusIcon = () => {
      if (icon === false) {
        return null;
      }
      if (isValidElement(icon)) {
        return icon;
      }
      if (icon === true) {
        switch (status) {
          case "success":
            return /* @__PURE__ */ jsx(RiCheckLine, { "aria-hidden": "true" });
          case "warning":
            return /* @__PURE__ */ jsx(RiErrorWarningLine, { "aria-hidden": "true" });
          case "danger":
            return /* @__PURE__ */ jsx(RiAlertLine, { "aria-hidden": "true" });
          case "info":
          default:
            return /* @__PURE__ */ jsx(RiInformationLine, { "aria-hidden": "true" });
        }
      }
      return null;
    };
    const statusIcon = getStatusIcon();
    return /* @__PURE__ */ jsxs(
      "div",
      {
        className: classes.root,
        ref,
        style: { ...style, ...utilityStyle },
        ...dataAttributes,
        ...restProps,
        "data-has-description": description ? "true" : "false",
        children: [
          /* @__PURE__ */ jsxs("div", { className: classes.contentWrapper, children: [
            loading ? /* @__PURE__ */ jsx("div", { className: classes.icon, children: /* @__PURE__ */ jsx(
              ProgressBar,
              {
                "aria-label": "Loading",
                isIndeterminate: true,
                className: classes.spinner,
                children: /* @__PURE__ */ jsx(RiLoader4Line, { "aria-hidden": "true" })
              }
            ) }) : statusIcon && /* @__PURE__ */ jsx("div", { className: classes.icon, children: statusIcon }),
            /* @__PURE__ */ jsxs("div", { className: classes.content, children: [
              title && /* @__PURE__ */ jsx("div", { className: classes.title, children: title }),
              description && /* @__PURE__ */ jsx("div", { className: classes.description, children: description })
            ] })
          ] }),
          customActions && /* @__PURE__ */ jsx("div", { className: classes.actions, children: customActions })
        ]
      }
    );
  }
);
Alert.displayName = "Alert";

export { Alert };
//# sourceMappingURL=Alert.esm.js.map
